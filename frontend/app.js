const API_BASE=window.API_BASE||"https://YOUR-RENDER-SERVICE.onrender.com";const $=x=>document.getElementById(x);const map=L.map("map").setView([22.5726,88.3639],12);L.tileLayer("https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png",{attribution:"© OpenStreetMap"}).addTo(map);let marker=L.marker([22.5726,88.3639]).addTo(map);async function get(u,o){let r=await fetch(API_BASE+u,o);if(!r.ok)throw Error(await r.text());return r.json()}async function loadHistory(){try{let rows=await get("/api/history?limit=10");$("history").innerHTML=rows.map(r=>"<tr><td>"+new Date(r.timestamp).toLocaleString()+"</td><td>"+r.congestion_level+"</td><td>"+r.speed.toFixed(1)+"</td><td>"+Math.round(r.confidence*100)+"%</td></tr>").join("")}catch{}}async function predict(){try{let lat=+$("lat").value,lon=+$("lon").value,speed=+$("inputSpeed").value,free=+$("freeFlow").value,weather=+$("weather").value;let p=await get("/api/predict",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({latitude:lat,longitude:lon,speed,free_flow_speed:free,weather})});$("level").textContent=p.congestion_level;$("predicted").textContent=p.predicted_speed+" km/h";$("confidence").textContent=Math.round(p.probability*100)+"%";marker.setLatLng([lat,lon]);map.setView([lat,lon],12);$("status").textContent="Prediction updated.";loadHistory()}catch(e){$("status").textContent=e.message}}$("predict").onclick=predict;$("refresh").onclick=predict;loadHistory();